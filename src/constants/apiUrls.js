export const apiUrl = 'https://foxtok-api.onrender.com';

export const postsURL = `${apiUrl}/posts`;
export const authURL = `${apiUrl}/auth`;
export const refreshTokenURL = `${authURL}/refresh-token`;
export const usersURL = `${apiUrl}/users`;